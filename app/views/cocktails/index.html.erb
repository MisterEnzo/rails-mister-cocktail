
<div class="container">
  <h1 class="text-center">INDEX</h1>
  <%= link_to "Add a new cocktail", new_cocktail_path, class: "btn btn-inverse text-center" %>
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3">
      <% @cocktails.each do |cocktail| %>
      <p><%= cocktail.name %></p>
      <% end %>
    </div>
  </div>
</div>

